= form_for :user, url: '/users' do |f|

  %p Email:
  = f.text_field :email
  %p Password:
  = f.password_field :password
  %p Password Confirmation:
  = f.password_field :password_confirmation
  = f.hidden_field :role, { :value => "copywriter" }
  %br
  = f.submit "Submit"

